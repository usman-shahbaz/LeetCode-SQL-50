def longestCommonPrefix(strs):

    ml = float('inf')

    for s in strs:
        if len(s) < ml:
            ml = len(s)

    i = 0
    while i < ml:
        for s in strs:
            if s[i] != strs[0][i]:
                return strs[0][:i]
        i += 1
        
    return strs[0][:ml]
