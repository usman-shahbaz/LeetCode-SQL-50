
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:

        f = defaultdict(list)

        for i in strs:

            k = ''.join(sorted(i))

            f[k].append(i)

        return list(f.values())
